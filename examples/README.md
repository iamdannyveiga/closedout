# Examples

Real receipts from the first build (a ledger schema and a phone-sized status page
that failed inspection on horizontal overflow, was fixed, and passed) will be
added here, one file per loop, each holding the brief that was sent, the worker
output it produced, the two inspector verdicts as they were written, and the
verification command with its raw output, including the FAIL round and the fix
that followed it, because a transcript that shows only the clean pass is
marketing and this file is not marketing.

<!-- EXAMPLES_PENDING -->
